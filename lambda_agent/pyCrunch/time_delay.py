def learn_time_delay(a, b):
    '''
    Given a set of data, need to learn the parameters of it's gamma

    :a    -> entrance node on intersection
    :b    -> exit node on neighbors intersection

    TODO: Make this do stuff
    '''

    shape = 3
    loc = 80
    scale = 4

    return (a, b, (shape, loc, scale))